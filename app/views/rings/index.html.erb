<% @rings.each do |ring| %>
    <ul>
        <li><%= ring.name %> - $<%= ring.price %></li>
    </ul>
    <button class="button">Click</button>
<% end %>

AVERAGE PRICE OF ALL RINGS: <%= @average_price %>